export type ImageThumbnail = {
    id: number,
    src: string,
    alt: string
}

export type CartItems = {
    id: string,
    productName: string,
    image: string,
    quantity: number,
    productPrice: number
}